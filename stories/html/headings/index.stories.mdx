import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import headingsContent from './headings.html'

<Meta title="Headings" />

# Headings

Headings are title used in the backoffice.

<Canvas>
  <Story name="Headings" parameters={{ docs: { source: { code: headingsContent } } }}>
    {() => headingsContent}
  </Story>
</Canvas>

