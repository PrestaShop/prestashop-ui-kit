import { useEffect } from 'react';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import tooltipsContent from './tooltips.html'

<Meta title="Tooltips" />

# Tooltips

Here are every texts components you can use.

<Canvas>
  <Story name="Tooltips" parameters={{ docs: { source: { code: tooltipsContent } } }}>
    {() => {
      useEffect(() => {
        window.prestaShopUiKit.init();
      });
      return tooltipsContent;
    }}
  </Story>
</Canvas>

